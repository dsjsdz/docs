# 16. 一键清除货道故障

:::tip
统一推送主题：`/clients/{machine_no}`, 其中 `machine_no` 为设备编码。
:::

## 请求示例

```json
{
  "timestamp": "1748331099",
  "machine_id": "10009",
  "method": "client.channel.fault.clear",
  "sign": "XXXXXXX",
  "client_id": "16327138",
  "product_ids": "1,2,3",
  "floor_status": "1",
  "status": "0"
}
```

## 参数说明

| 字段名          | 默认值                  | 是否必须 | 描述                    |
|--------------|----------------------|------|-----------------------|
| timestamp    | -                    | ✅    | 当前时间戳(秒级)             |
| machine_id   | -                    | ✅    | 设备 id(主键)             |
| method       | client.factory.reset | ✅    | MQTT 方法名              |
| product_ids  | 1,2,3                | ✅    | 需要清除故障的产品ID，以逗号分隔的字符串 |
| floor_status | 1                    | ✅    | 1: 正常（固定值）            |
| sign         | -                    | ✅    | 签名字符串                 |
| client_id    | -                    | ✅    | 设备编码，即 `machine_no`   |
| status       | 0                    | ✅    | 处理状态，默认：0             |

💡 注意：`product_ids` 需要清除故障的产品ID，以逗号分隔的字符串。

> `floor_status` 目前是固定传值。
